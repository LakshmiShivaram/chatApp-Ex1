// import { createReducer, on, State } from '@ngrx/store';
// import { Ichats } from './ichats';
// import { addChat } from './message-action';

// export const initialState: ReadonlyArray<Ichats> = [];

// export const messageReducer = createReducer(
//   initialState,
//   on(addChat, (state, { id }) => [...id])
// );
